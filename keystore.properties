# Keystore configuration for release builds
# These values are used by GitHub Actions to sign the APK
# DO NOT commit the actual keystore file - use GitHub Secrets instead

STORE_FILE=release.keystore
STORE_PASSWORD=android
KEY_ALIAS=release
KEY_PASSWORD=android
